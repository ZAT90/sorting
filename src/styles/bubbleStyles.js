import React from 'react';
import { StyleSheet } from 'aphrodite';

const styles = StyleSheet.create({
    buttonDiv: { display: 'flex', justifyContent: 'center', alignItems: 'center', height: '10vh', width: '80vh' }

})

export default styles